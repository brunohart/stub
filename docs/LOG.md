# Build log

Newest at the top. One entry per slot. What shipped, what broke, what the reader did.

## 2026-09-16 — Days 8–10

Playbook complete, nothing scheduled. Days 8, 9 and 10 had no playbook entry and no Linear backlog for the project; nothing was built. The Day 7 "still rough" list (the two Vision requests measured against each other, the span in words in the season brief, "Aftrsun", a script for the README's eval table, the privacy manifest) is where a Day 8 would start if this file grows one.

## 2026-09-13 — Day 7

**Shipped.** The write-up. `README.md` rewritten: the technology table now names the crop, the season, the reach, the camera and the accessibility pass with their ADRs; a seven-day table with every screenshot; the run instructions with the flags `run.sh` grew over the week; the Day 6 eval table; an honesty section that tells the Day 0 model failure, the Day 1 segmentation failure and how each resolved, and says what is not proved. `docs/post.md`: the draft post on three things, evidence first. Guided generation: the model is a reader not a parser, scored in the drawer's shapes, hinted by the heuristic, and not the same reader twice; the season sentence as the second use, with the rule-on-the-way-out lesson. `RecognizeDocumentsRequest` against `RecognizeTextRequest`: the honest finding is that the document request's order has been right every time and the fallback has never been needed in the simulator, and the real Vision story was the segmentation strip and the second detector. Metal `[[stitchable]]`: the silkscreen shader in full, why the look was specified in SwiftUI first and what that bought (the side-by-side, the widget). `docs/device.md`: the four things only a phone can prove and what each needs. Signing: `DEVELOPMENT_TEAM` in the base settings of `project.yml`, regenerate, run from Xcode, and what to change if the App Group identifier is refused. Apple Intelligence on the phone, with the 26.5-runtime note for this Mac. A test order: camera, Shortcuts, lock-screen widget, the icon at 60pt, the feel, streaming on the Neural Engine. TestFlight: not ready, not far; the archive script, a privacy manifest, the App Store Connect record and the build number are the whole list. Tag `v0.1.0`. No code changed today; the floor was green from Day 6's run (48 tests). Screenshot `docs/screenshots/day-7-table.png`: the finished drawer, eight stubs, all read by the model.

**Reader.** All eight seeded stubs filed `by foundation-models (100%)`, cropped by rectangles, the same eight names and seats as Day 6, "Aftrsun" included. The season was accepted on the first attempt, twelve words in 7.8 s: *"You've gone to eight cinemas in the last year, mostly on Sundays."* Every rule kept. "In the last year" is loose (the stubs span February 2024 to March 2025 and one in September 2026), which is the same failing as Day 6's "twice a week" in a milder form: the brief gives the model months and counts but never the span, and the model fills the gap with a phrase.

**Broke and fixed.** Nothing broke. One sentence in the first draft of the README dated the simulator-runtime finding to a "Day 8" that does not exist; it was found on 8 September and is recorded in the memory notes, not the log, and the README now says what was found without a day.

**For Bruno.** Everything in `docs/device.md`, in that order. The post is a draft with your voice guessed at from the log; the three section claims are all backed by lines in `docs/LOG.md` and `docs/evals.md`, so cut freely but check against those before adding. Tag `v0.1.0` is pushed; the version in `project.yml` already reads `0.1.0`.

**Still rough.** The post's second section is thin on its stated subject: the two Vision text requests were never measured against each other, and the section says so; a Day 8 that runs both on all eight fixtures and a real photograph would give it a table. The brief for the season sentence should state the span in words ("over two years") so the model stops inventing one, and `SeasonRules` should refuse a frequency or span claim that is not in the brief. "Aftrsun" is four days running. The README's eval table is the Day 6 run pasted by hand; it will drift from `docs/evals.md` unless a script writes it. There is no privacy manifest yet. The playbook is complete; the launchd slot will now log "playbook complete, nothing scheduled" until this file grows a Day 8.

## 2026-09-12 — Day 6 (run 2026-09-13; the Day 6 slot did not fire)

**Shipped.** The camera path, gated: `StubScanner` wraps VisionKit's `DataScannerViewController` and is offered on the import screen only when `isSupported && isAvailable`, so the simulator keeps the Photos path (ADR-003). The live lines are put in reading order (top to bottom, then left to right, repeats and blanks dropped) and handed to the parsers as a `StubReading` through the new `StubReader.understand`, pulled out of `read(_ image:)`: floor first, then the model with the floor's draft in hand (ADR-010), exactly as a photograph goes. The frame captured at that moment is the plate. A stub kept from the scanner goes through `ImportView.keep` into the context, so the table's `stubs` query fires and the widget is reloaded (ADR-012); nothing writes to the store behind the table's back. The icon: `scripts/make-icon.swift` draws parchment with grain, one orange stub tilted −4°, notched and perforated, a faint ink pass out of register beneath it, into `Stub/Resources/Assets.xcassets`; opaque RGB, as an icon must be. The accessibility pass: each card is one VoiceOver element, a button, that says "The Brutalist, 6 September 2026, Embassy Theatre, seat H12" (`Stub.spokenLabel`; the printed "06 SEP 26" is not read letter by letter) and opens on double-tap through `accessibilityAction`, since the press gesture keeps it from being a `Button`; the import fields carry their labels; the detail's figures read as "Seat, H12" and stack at the accessibility sizes; the delete button, the season sentence and every import button are 44pt tall; Reduce Motion drops the settle spring on the import plate as it already did everywhere else. Titles never truncate: the card's `lineLimit(2)` is gone. At the five accessibility sizes the table is one column (ADR-013); at every standard size it stays two. `run.sh` learned `--import` and `--type <size>`. Forty-eight tests (four new in `ScannerTests`: ordering, de-duplication, the hand-off to the heuristic with no cropping stage, the spoken label). Screenshots: `docs/screenshots/day-6-type-xxl.png` (the seeded table at xxLarge, two columns, "Event Cinemas Queen St" wrapping), `day-6-type-ax.png` (accessibility-extra-large, one column), `day-6-import.png` (the import sheet on the simulator, no camera button, "On-device model ready"), `day-6-icon.png` (the icon on the home screen beside the Day 5 widget).

**Reader.** All eight seeded stubs filed `by foundation-models (100%)`, cropped by rectangles: The Brutalist / H12, Perfect Days / F7, Dune Part Two / D4, Past Lives / K9, No Other Land / J14, **Aftrsun** / E11 (the model's spelling again, hint and all), Anora / C8, La Chimera / F12. Streaming: title at 1.1–10.9 s, done at 3.0–17.7 s per stub. The season, first launch: refused twice at twenty-eight words and the hand sentence stood ("Eight stubs across eight cinemas, most of them in March."). On the next launch (the drawer unchanged, the cache holding nothing because nothing was accepted) the model was asked again and accepted at eighteen words: *"You have gone to the cinema twice a week, mostly on Sundays, and have seen eight different films."* Every rule kept; "twice a week" is invented (eight films over two years), a claim the brief never made. The eval table ran once on today's build: heuristic 8/8 everywhere; cold model title 8/8, cinema 6/8, date 5/8, seat 8/8; hinted model title 7/8 (Aftrsun), cinema 8/8, date 8/8, screen 8/8, seat 8/8. Median 8.6 s hinted, 8.9 s cold.

**Broke and fixed.** The icon rendered solid black on the first pass: `NSImage.lockFocus` drew fine, but the re-draw into an opaque `NSBitmapImageRep` to strip the alpha lost everything, and the second attempt (an `NSGraphicsContext` over an alpha-less rep) returned no context at all. It draws straight into a `CGContext` with `noneSkipLast` now. The notches spilled: even-odd over a rectangle and two circles fills the halves of the circles outside the rectangle too; the plate is clipped to the card first. `run.sh` had never been launched without app arguments: `"${ARGS[@]}"` on an empty array is unbound under `set -u`, so the first accessibility-size run died before install; `${ARGS[@]+"${ARGS[@]}"}` is the idiom. `StubScanner.isUsable` is main-actor and the `PhotosPicker` label is a Sendable closure, a warning; the value is read once into a local. The unified log dropped the probe line for the seeded run, as it does; the model plainly answered.

**For Bruno.** The camera is a device test: point it at a real stub, watch the line count settle, tap "Read this stub", and see whether the live lines read as well as a photograph's (`StubVision` orders Vision's lines by their boxes; the scanner's order is mine). The icon is worth a look at 60pt on a real home screen; the notches may be too small there.

**Still rough.** The scanner's reading order groups lines within 12 points of each other as one row; the threshold is a guess made without a viewfinder, and a receipt in a small face may need less. The scanner reads whatever the camera sees, the table included; a region of interest drawn as a ticket-shaped frame would help both the person and the parser. `capturePhoto()` returns the full frame, not the ticket, so a scanned card's plate is uncropped where a photographed one is cropped; running `StubCrop` on the frame would fix it and costs nothing. "Twice a week": a rule against frequency claims that are not in the brief belongs in `SeasonRules`, or the brief should state the span in words. "Aftrsun" is now three days running; the hint's title when the model's is one edit away (Day 5's note) is overdue. The `Named` and `Count` views on the season sheet, and the widget, were not part of today's pass. Increase Contrast and Bold Text are untested. Nothing proves the one-column table beyond a screenshot; a test on `DynamicTypeSize.isAccessibilitySize` would be a test of Apple's enum.

## 2026-09-11 — Day 5

**Shipped.** Two verbs for the outside: `LogStubIntent` opens the drawer straight onto the import sheet (`Reach.wantsImport`, watched by the table), `FilmsThisYearIntent` counts the calendar year's stubs and answers in words ("Two films this year."); `StubShortcuts` gives them the phrases "Log a stub in Stub" and "How many films this year in Stub". The store moved to `SharedStore.url`, `Stub.store` inside the `group.com.designedbybruno.stub` container, falling back to the app's own Application Support when the process has no group; a pre-Day-5 drawer is moved across once. The widget (`StubWidget` target, `systemSmall`, `accessoryRectangular`, `accessoryInline`) opens the same file read-only and prints the last stub kept: a cream card tilted −0.8°, the orange plate a little out of register beneath it, title in Host Grotesk, date and seat in Fragment Mono, cinema in grey; one timeline entry, no schedule, reloaded by the table's `.task(id: summary.key)` (ADR-012). The Day 4 carry-over first: in `ModelParser.draft` a seat without a row takes the hint's when the hint has one, and a shouted title or cinema gets the heuristic's title-case. `DisplayTitle` and the font audit moved out of `Type.swift` so the widget can compile it. Forty-four tests (four new in `ReachTests`, two in `ModelParserTests`). Screenshots `docs/screenshots/day-5-reach.png` (the table), `day-5-shortcuts.png` (the Shortcuts app listing both phrases under Stub), `day-5-widget.png` (the small widget on the simulator's home screen, reading the seeded drawer).

**Reader.** The probe passed in the app run and all eight seeded stubs filed `by foundation-models (100%)`, cropped by rectangles: La Chimera / **F12** (the hint's row won; Day 4 filed "PLACE 12"), Anora / C8, **Aftrsun** / E11, No Other Land / J14, Past Lives / K9, Dune Part Two / D4, Perfect Days / F7, The Brutalist / H12. "Aftrsun" is the model's own spelling of the low-contrast fixture, hint and all; `cased` only fixes shouting, not letters. Streaming: title at 7–14 s, done at 15–36 s per stub. The season: attempt one refused at twenty-three words, attempt two accepted in 12.5 s: *"You went to the cinema eight times in two years, mostly on Sundays."* The voice is "you" now, which Day 4 left unproved. The eval test ran three times today: the first (before the rerun) had the hinted model at title 7/8 with the same "Aftrsun"; the second timed out the probe in the test host and wrote a heuristic-only table, discarded; the committed table is the third. Heuristic 8/8 everywhere; cold model title 8/8, cinema 7/8, date 7/8, screen 7/8, seat 7/8, price 2/8 (USD for every `$`, as on Day 3); hinted model 8/8 on every field except screen 7/8 ("Salée 2" for La Chimera's SALLE 2, a spelling `normalisedScreen` does not know). The hinted seat is 8/8 for the first time. Median 8.7 s hinted, 8.9 s cold; max 11.7 s hinted, 9.0 s cold.

**Broke and fixed.** `theAnswerIsSpokenInWords` failed on the first run: `.capitalized` turns "twenty-three" into "Twenty-Three"; only the first letter goes up now. The widget's first home-screen render broke "24 MAR 24 · F12" at the dot, and once the numbers were pinned to one line "La Chimera" wrapped instead: the system's content margins take about a fifth of a small widget, so the card disables them and insets itself by ten. The gallery preview renders wider than the placed widget, so a preview is not a proof; the shot is of the placed one. Running "Films this year" from the Shortcuts app fails in the simulator with "Unable to run App Shortcut": `linkd` rejects the app's XPC client ("Not a platform binary … Unable to get teamId") and then "Couldn't find AppShortcutsProvider". The build has no `DEVELOPMENT_TEAM` and the simulator binary is unsigned; the phrases are listed, the run is a device test. The unified log dropped the first three stubs' seed lines and the `Store:` line for the run, as it did on Day 4, so the store's location was checked on disk instead: `Stub.store` and its WAL are in `Containers/Shared/AppGroup/…` and the app's Application Support is empty.

**For Bruno.** Two things only a signed build can prove: say "How many films this year in Stub" to Siri (or run it from Shortcuts) on your phone, and put the widget on the lock screen; the accessory families are unproved here (only `systemSmall` could be placed). Both need a `DEVELOPMENT_TEAM` in `project.yml`, which Day 7's device notes will spell out.

**Still rough.** The intents are unproved end to end and the lock-screen families are unproved at all, both for signing. The widget's gallery preview shows the placeholder ("Past Lives") whatever the drawer holds; that is by choice, but a real last card would be the better advertisement. "Aftrsun": the model misspells a title with the hint in front of it, and the hint is right; a rule that takes the hint's title when the model's is one edit away belongs in the eval table before the drawer. "Salée 2" is a screen the normaliser should learn. A timed-out probe still locks the test host to heuristics for the whole run (Day 4's note; hit again today, and the table had to be rerun). The season's first attempt was refused again (twenty-three words); the retry cost 12.5 s. An intent that files a stub straight to the store from outside the table would have to call `Reach.refreshWidgets()` itself; nothing does that yet.

## 2026-09-10 — Day 4

**Shipped.** The one italic sentence is written by the on-device model, from a drawer it never sees. `SeasonSummary` counts the drawer by hand (films, distinct cinemas, most visited, busiest month and weekday, total paid by currency) and writes `handSentence`; the model is given only the `brief` (one fact per line) and asked, through `@Generable struct Season`, for one sentence. `SeasonRules.judge` refuses anything over nineteen words, with an exclamation mark, an emoji, a digit or a second sentence; a refused sentence earns one retry with the reason in the prompt, then the hand sentence stands. The accepted sentence is cached in `UserDefaults` against `SeasonSummary.key`, FNV-1a over the brief, so a drawer is phrased once and the cache survives a relaunch (ADR-011). `SeasonModel` waits `1.5 s` for the drawer to sit still and then for `StubReader.isBusy` to clear, so a seed of eight produces one sentence at the end and an import never queues behind it. The season sheet (`SeasonView`): the sentence in Newsreader italic 26, the numbers in Fragment Mono (a cinema name in Host Grotesk, because a name is not a number), nothing else; the sentence in the header is now a button that opens it. Debug `-season` opens the sheet two seconds after the last seeded stub; `scripts/run.sh --wait-for <regex>` polls the app log instead of sleeping a fixed settle, and its log summary starts at launch. `ModelProbe.explain` now reads a `LocalizedError`'s description (our own failures logged as `ModelFailure 1` before). Forty-six tests. Screenshot `docs/screenshots/day-4-season.png`.

**Reader.** The probe passed (`Model answered 'Ready' in 7010 ms`). All eight seeded stubs filed `by foundation-models (100%)`, cropped by rectangles: The Brutalist / H12, Perfect Days / F7, Dune Part Two / D4, Past Lives / K9, No Other Land / J14, Aftersun / E11, Anora / C8, La Chimera / **PLACE 12** (the model dropped `RANG F` this run and `normalisedSeat` passed "PLACE 12" through; the hint had F12). Streaming: done at 2.5–27.6 s per stub, median about 14 s, against Day 3's 33–39 s; the Metal-to-CPU fallback is the same, so the difference is the host's mood, not ours. The season: attempt one refused at twenty-three words ("The person went to cinema eight times in a year, mostly on Sundays and Tuesdays, with a few exceptions in Febru…"), attempt two accepted in 28.3 s total: *"The person went to cinema eight times in a year, mostly on Sundays and Tuesdays."* Every rule kept; the voice is wrong ("the person"), and the instruction now says to address the owner as "you" or leave the subject out, unproved by a run. The eval test ran twice on today's assets; the committed table is the second. Heuristic 8/8 everywhere; cold model cinema 7/8, date 7/8, seat 7/8, price 2/8; hinted model 8/8 except seat 7/8 (the La Chimera seat again, "PLACE 12" with the hint saying F12). The first run had the hinted model at screen 7/8 and seat 8/8 instead, so the sample is still a sample. Median 9.1 s hinted, 8.8 s cold, cold max 12.1 s.

**Broke and fixed.** The model's sentences broke the rules before they kept them: the first for the seeded drawer was a weekday inventory (seventeen words, accepted), the next three ran to twenty-three, twenty-four and twenty-five words and were refused by `SeasonRules`; the retry with the reason fed back is what got an accepted sentence. The probe timed out at 12 s twice today (once in the test host, once when a run launched while the previous run's model work was still being torn down) and the whole run fell to heuristics, honestly; the window is now 20 s. `-reset` used to empty the drawer inside the seed, which waits for the probe, so for the first seven seconds the table counted yesterday's drawer and started phrasing it; the drawer is emptied in `StubApp.init` now, and the season cache with it. The first `--wait-for` matched `cached` from the same pre-reset drawer and photographed five seconds in; the pattern for the daily shot is `written|hand-counted`. My retry prompt was written with a `\` continuation in a single-line literal and the build failed three times while `| tail -1` hid the exit code; `run.sh` then installed the previous binary and I read its log as the new one's for a run. The unified log dropped every info-level line for one process (only the season's notice and error lines survived), which is why the seed log for that run looked like two stubs; the sim's log store is not a witness under pressure, and the daily shot should be read from `run.sh`'s own capture.

**For Bruno.** Nothing only you can do. The sentence is worth reading on a device with your real drawer: the fixtures give it eight cinemas for eight films and two years, which is a thin season to be dry about.

**Still rough.** The voice: "the person" is the model's, the fix in the instructions is unproved. `normalisedSeat` accepts "PLACE 12" as a seat; when the model's seat has no row and the hint's has, the hint should win (ADR-010 says the model is scored in the drawer's shapes, so the shape check belongs in `ModelParser.draft`). The model read "AFTERSUN" and "RIALTO CINEMAS NEWMARKET" in capitals on one run; title-casing the model's title and cinema the way the heuristic does belongs in the same place. A refused sentence costs a second model call (28 s for the seed's sentence); the rules could be softened by trimming a trailing clause rather than asking again. The season is not in the eval harness; a `docs/evals.md` row with acceptance rate over, say, five sentences would make "the model keeps the rules about half the time cold" a measurement instead of today's anecdote. Two runs in a row hit the 12 s probe timeout; if 20 s is hit too, a timed-out probe should stay `.untested` rather than lock the session to heuristics.

## 2026-09-09 — Day 3

**Shipped.** The model reads properly, and the day the model first answered. `ModelParser.stream` wraps `streamResponse(to:generating:)` and yields a `StubDraft` for each changed snapshot of the partially generated struct, so the title lands in the import fields before the seat (`StubReader.read(_:progress:partial:)`, `ImportView` keeps the fields on screen while the stage is *understanding*). The heuristic runs first, always, and its draft goes into the prompt as a hint ("keep what the text confirms, correct what it does not, fill what it missed"). The model's fields are put into the drawer's shapes before anyone scores them: `normalisedScreen` ("SCR 2" → "Screen 2"), `normalisedSeat` ("SEAT D 4" → "D4"), comma prices, and `wallClock`, which drops any zone designator because a ticket prints the time on the wall of the cinema. Eval harness: `fixtures/expected.json` is the truth for eight fixtures; `StubTests/EvalTests.swift` runs each through crop → Vision → heuristic → cold model → hinted model and writes `docs/evals.md` (hit rate by field, latency, per-fixture table). Four harder fixtures from `scripts/make-fixtures.swift`: rotated 12°, pale ink on greyed paper, a thermal receipt with an address line and GST/TOTAL lines, and a French ticket with accents, `24.03.2024`, `RANG F PLACE 12` and `12,50 €`. The heuristic learned `SALLE`/`SAAL`/`SALA`, `RANG`/`REIHE`/`FILA` and `PLACE`/`PLATZ`/`SIÈGE`, diacritic folding, French and German articles in title case, and that a street address is never a film. ADR-010. Thirty-one tests. Screenshot `docs/screenshots/day-3-evals.png`.

**Evals.** `docs/evals.md`, model present. Heuristic 8/8 on all six fields in about a millisecond. Cold model, the committed run: title 8/8, screen 8/8, seat 8/8 after normalisation, date 7/8 (`12/03/2024` read as 3 December), cinema 6/8 ("Queen St" for Event Cinemas Queen St, "The Roxxy Cinema"), price 2/8 (it says USD for five of the `$` tickets and AUD for one). Hinted model: 8/8 on every field. Median latency 8.7 s hinted against 8.9 s cold, so the hint is free. The model is not the same reader twice: the run before this one had the cold model at cinema 5/8 and date 6/8, and the hinted model at title 7/8 ("Aftrsun" off the low-contrast stub, which the cold model and the regex both got), so the table is a sample, not a spec. Before the normalisation and wall-clock fixes the first table read: cold model date 0/8 (every one twelve or thirteen hours late, the model writes `Z`), screen 4/8, seat 6/8, and those misses were "SCR 2", "Cinema 2", "SEAT D 4", "RANG F PLACE 12": read correctly, left as printed. That table is why the model is scored in the drawer's shapes and not on strings.

**Reader.** The probe passed for the first time: `Model answered 'I' in 5406 ms`, status line "On-device model ready". Seeded stubs are filed `by foundation-models (100%)`: The Brutalist / Embassy Theatre / H12, Perfect Days / Lighthouse Cinema Cuba / F7, Dune Part Two / The Roxy Cinema / D4, Past Lives / Penthouse Cinema / K9, all cropped by rectangles, Vision 10–13 lines. Streaming in the simulator is slow: title at 7.6–17 s, done at 33–39 s for three of the four, 8.4 s for the fourth; Metal reports `Cannot create MPS context, fallback to CPU` for the model. The eval's whole-response calls take 9 s median on the same host, so the streamed path is paying for something the whole path is not (see *Still rough*). The seed now walks every PNG in the bundle, so it seeds eight; the screenshot was taken at four.

**Broke and fixed.** The slot stalled twice before it ran: the eval test asks the model sixteen times at up to fifteen seconds each while `scripts/test.sh` filters xcodebuild down to a handful of lines, and ten silent minutes is a stall to the agent watchdog. The test itself is fine; the slot now knows to run it in the background and poll. `test.sh` retried a *failed* run three times: with `pipefail`, xcodebuild's exit 65 on a red test made the whole pipeline false and fell through to "did not run"; the verdict is now read from the log, not the pipe. `draft.confidence` was `0.9999999999999999` for a full model read (0.5 + 0.2 + 0.1 + 0.1 + 0.1 in binary) and the label would have said 99%; both parsers now round to hundredths. The Day 1 strip came back a second way: 33 pixels in from the left, clear of the 28-pixel edge tolerance, touching two edges and accepted, and the crop had no text; a quadrilateral spanning more than 95% of the frame is now rejected too ("spans 98% of the frame").

**For Bruno.** Nothing new. The model answered because the host's assets finally arrived (Days 0–2 asked you to turn on Apple Intelligence; either you did or the catalog caught up). Streaming on a device is the one to feel: the simulator's CPU fallback is not the phone.

**Still rough.** Streaming is four times slower than a whole response in the simulator for three of four stubs; whether that is the CPU fallback, per-snapshot overhead, or the hint's longer prompt is unmeasured, and the eval only times whole responses (add a streamed column on a device day). The seed reads eight stubs at ~35 s each, so `run.sh` at its default 12-second settle photographs an empty drawer; pass `STUB_SETTLE` or take the shot after the log says `done`. The model still guesses USD for `$` cold; the hint fixes it, a currency hint from the locale might fix it colder. The hinted model has re-read a title the hint had right ("Aftrsun", the run before the committed one); the prompt could say the hint's title is usually right. One eval run is one sample of a non-deterministic reader; the harness should run each fixture three times and report the worst before any regex is loosened on its evidence. `EvalTests` writes into the repo via `#filePath`, which is honest in the simulator and wrong on a device.

## 2026-09-08 — Day 2

**Shipped.** Card → detail is a `navigationTransition(.zoom)` from a `matchedTransitionSource` on each card; the detail arrives at the stub's tilt and settles to straight on the same clock as the zoom. `Motion.stamp` retuned (response 0.34, damping 0.55: one swing about a fifth past the target, the second under a hundredth), `Motion.plain` (critically damped) for Reduce Motion, which also never tilts. Holding the photograph in the detail lifts the silkscreen and slides the orange plate from its 6×7 misregistration into register; the haptic fires when the spring is logically complete, not on touch-down. The empty drawer's blank stub inhales once on appear (one spring up, one settle down, nothing under Reduce Motion). The 12-second clip is `docs/screenshots/day-2-press.mov` (HEVC, half size, 3.5 MB; the grain makes anything larger balloon); stills are `day-2-table.png`, `day-2-detail.png`, `day-2-detail-held.png`. `scripts/run.sh --drive` (`DebugDrive`) presses, opens, holds and closes on a clock from launch so the simulator can be filmed without hands. Twenty-one tests.

**The column question, answered.** Day 1 asked whether the table wants one wider column for landscape stubs. It does not: since Day 0 the whole table had been about 232pt wide on a 402pt screen. The `VStack` inside the `ScrollView` shrank to its widest non-greedy child (the one-line season sentence, "Four stubs across four cinemas."), and `containerRelativeFrame` sized the columns against that. On Day 0 the sentence was longer, so it was hidden. One `frame(maxWidth: .infinity)` and the plates are ~60% larger, the 1.15/0.85 overlap reads, and "Dune Part Two" fits on a line. Two columns stay (ADR-009).

**Reader.** Four fixtures, all cropped 2400×1400 → ~1963×842 by rectangles (segmentation still returns the bottom-quarter strip and is rejected for hugging three edges), Vision 10–13 lines each, all filed by heuristics at 100%: The Brutalist / H12, Perfect Days / F7, Dune Part Two / D4, Past Lives / K9. Model probe: `GenerationError -1 ← ModelManagerError 1026 ← UnifiedAssetFramework 5000`, same as Day 1; the status line says so and the reader does not ask the model.

**Broke and fixed.** The iOS Simulator tool in this session cannot drive the simulator (xcode-select points at the command-line tools and the fix needs sudo), so the tap-to-open and hold could not be verified by hand; `-drive` is the honest substitute and the clip is the proof. First cut of `DebugDrive` ran its whole choreography in zero milliseconds: it lived in the table's `.task(id: stubs.count)`, which the seed cancelled on every insert, and `try? await Task.sleep` on a cancelled task returns at once. Second cut was cancelled the moment the detail was pushed (the root's `.task` ends on disappear). It now runs in its own task on a clock from launch (`DebugDrive.curtain`, 16 s), and a filming script can be timed against it. The first "A/B" for the small Dune plate blamed `matchedTransitionSource`; it was innocent, the width bug above was the cause.

**For Bruno.** To let the tooling tap the simulator directly: `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer`. Apple Intelligence still needs turning on for the model probe to pass (Day 1 note stands). Feel the springs on a device: the stamp's overshoot is tuned by arithmetic, not by thumb.

**Still rough.** The table cards use `onTapGesture` next to the pressing gesture rather than a `Button`, so VoiceOver does not announce them as buttons (Day 6). The zoom-back on dismiss was not captured in the clip (the recording stopped a beat early). The card's press physics are barely visible at 0.4 s frame samples; the clip is best watched at speed. `-drive` only works with `-seed` fixtures and Past Lives is hard-coded as the stub it opens (the first in the drawer).

## 2026-09-07 — Day 1

**Shipped.** The look is drawn by Metal: `paper`, `silkscreen` and `misregister` from `Shaders/Silkscreen.metal` are the default, the Day 0 SwiftUI versions stay behind `LookEngine` (`scripts/run.sh --look swiftui`), and the pair of screenshots is in `docs/screenshots/day-1-look-metal.png` and `day-1-look-swiftui.png`. The reader crops: `StubCrop` finds the ticket's corners and Core Image straightens them, so the card plate is the ticket rather than the table, and the card takes the ticket's own shape. `ModelProbe` asks the model for one token at launch and the import screen's status line reads from that. Parser edge cases: "Row H Seat 12", "12,50 €", ISO currency codes, `dd.MM.yyyy`, dates without a year. Nineteen tests.

**Reader.** Four fixtures, all cropped 2400×1400 → 1963×842 **by rectangles**, all read from the crop (Vision 10–13 lines each), all filed by heuristics at 100%: The Brutalist / H12, Perfect Days / F7, Dune Part Two / D4, Past Lives / K9. Vision's document segmentation returns the same full-width bottom-quarter strip for every fixture at 0.83–0.99 confidence on this simulator; on the Mac the same request finds the ticket exactly. The strip is rejected ("hugs 3 frame edges") and `DetectRectanglesRequest`, which is arithmetic rather than a downloaded model, finds every ticket at 1.0 (ADR-007). The model probe failed in 1 s: `GenerationError -1 ← ModelManagerError 1026 ← UnifiedAssetFramework 5000 "no underlying assets for com.apple.modelcatalog"`. The status line now says "On-device model answered with an error; reading with heuristics" and the reader does not bother the model until a probe succeeds.

**Broke and fixed.** The floor was down when the slot started: three CodeSign failures with "resource fork, Finder information, or similar detritus not allowed". `~/Documents` is an iCloud file-provider domain and the provider stamps every `.app` package under it with `FinderInfo`, faster than a script can strip it and even inside a `.nosync` folder. Derived data now lives in `~/Library/Developer/Xcode/DerivedData/Stub-scripts` (ADR-008). The trailing-euro price regex had `\b` after `€`, which never matches at the end of a line. `Logger` will not interpolate an optional `String`. The first crop test failed honestly: the crop was a 2424×353 strip with no text in it, which is how the segmentation problem was found.

**For Bruno.** The model catalog on this Mac has no assets (`UnifiedAssetFramework 5000`): System Settings → Apple Intelligence & Siri, turn it on, let the download finish, then the probe will pass. The same host problem likely explains segmentation; on a device with the assets it should be the first detector that wins, and the seed log will say `[cropped by segmentation]`.

**Still rough.** The card plates are small at 2.3:1 in a half-width column; Day 2's zoom transition will decide whether that is right or the table needs one wider column for landscape stubs. `misregister` is applied only to `DisplayTitle`; the italic sentence and card titles are single-pass, as designed, but the Metal and SwiftUI plates differ slightly in grain scale (Metal is per-pixel, SwiftUI is a tiled plate). Nothing tests the Metal shaders themselves.

## 2026-09-06 — Day 0

**Shipped.** The whole skeleton: model, reader, two parsers, look, table, import, detail, seed path, fixtures, scripts, playbook, ADRs. Linear project created. Repo pushed.

**Reader.** Vision read 9–10 lines per fixture. Foundation Models reported `.available` and then failed all four calls with `GenerationError -1` wrapping `ModelManagerServices.ModelManagerError 1026`. Heuristics filed all four at 100% confidence: The Brutalist / Embassy Theatre / H12; Perfect Days / Lighthouse Cinema Cuba / F7; Dune Part Two / The Roxy Cinema / D4; Past Lives / Penthouse Cinema / K9.

**Broke and fixed.** Fonts with `[wght]` in the filename did not register from `UIAppFonts` (renamed). The misregistered plate rendered grey because the inner `foregroundStyle` won (now `DisplayTitle` draws both passes explicitly). Two-column table overflowed the screen because `scaledToFill` images had no width constraint (now `Color.clear.overlay`). Metal toolchain catalog fetch failed twice before succeeding.

**For Bruno.** Check System Settings → Apple Intelligence & Siri is on and the model has finished downloading; error 1026 from ModelManager on the simulator is usually the host's model assets. Once the model answers, the seed log will show `by foundation-models`.

**Still rough.** Cards show the table around the stub (crop is Day 1). Import screen says "On-device model ready" when it is not (Day 1). Metal shaders compiled but not active (Day 1).
